spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:15432/flower_delivery}
spring.datasource.username=${DB_USERNAME:flower}
spring.datasource.password=${DB_PASSWORD:flower1234}
spring.data.redis.host=${REDIS_HOST:localhost}
spring.data.redis.port=${REDIS_PORT:16379}

